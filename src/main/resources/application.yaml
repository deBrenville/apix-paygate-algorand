quarkus:
  http:
    port: 8080
  log:
    level: INFO

onramp:
  app-name: "APIX x402 Onramp"
  # Algorand network + facilitator (pinned during the x402 spike, Task 2)
  network: "algorand-testnet"
  facilitator-url: "https://facilitator.goplausible.xyz"
  # Price asset: "0" = native ALGO (default, no ASA opt-in). Set to the USDCa testnet ASA id to price in USDC.
  price-asset-id: "0"
